# Sidecar Python deps — the human-edited SOURCE list. Setup installs the
# compiled requirements.lock, not this file. After editing, regenerate it:
#   uv pip compile sidecar/requirements.txt --universal --python-version 3.13 \
#     -o sidecar/requirements.lock
khora[embedded]
fastapi
uvicorn[standard]
openai
httpx
# GGUF fetch/reuse via the shared HuggingFace hub cache (electron/main.js spawns
# download_models.py). Present transitively through khora[embedded], but pinned
# here because it's load-bearing for model download.
huggingface_hub
pillow
pillow-heif
python-multipart
reverse_geocoder
pycountry
